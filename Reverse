def reverse(self,s):
  word = s split()
  reverse = word[::-1]
  reverse_st = ''.join(reverse)
  return reverse_st


//Java

String s = "Suppose";
output:- "esoppuS";
rev = "";

for(int i=0; i<s.length(); i++)
{
  rev += s.charAt(i);
}
return rev;
